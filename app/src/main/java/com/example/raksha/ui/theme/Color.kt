package com.example.raksha.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Girly-pink but professional palette:
 * - soft blush background
 * - deep berry primary
 * - clean surfaces
 * - readable text
 */

// Brand (pink / berry)
val PinkPrimary = Color(0xFFE11D8B)   // classy pink (not neon)
val PinkPrimaryDark = Color(0xFFBE185D)
val PinkSecondary = Color(0xFFF472B6) // soft pink
val PinkAccent = Color(0xFFFB7185)    // rose accent

// Surfaces
val AppBackground = Color(0xFFFFF7FB) // very light blush
val CardSurface = Color(0xFFFFFFFF)   // white
val CardSurfaceSoft = Color(0xFFFFECF6) // light pink card

// Text
val TextMain = Color(0xFF111827)      // near-black
val TextMuted = Color(0xFF6B7280)     // grey
val OnPink = Color(0xFFFFFFFF)        // white text on pink buttons

// Status
val Danger = Color(0xFFDC2626)
val Success = Color(0xFF16A34A)
